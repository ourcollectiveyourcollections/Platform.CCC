# Scripts

These scripts are intentionally lightweight and local-first.

They should not store private keys or modify live production records without review.

## Scripts

- `validate_metadata.py`: validates JSON files against expected required fields.
- `build_mint_pack.py`: builds a local mint-pack folder from metadata inputs.
- `export_dashboard_json.py`: converts registry CSV snapshots into dashboard JSON.
